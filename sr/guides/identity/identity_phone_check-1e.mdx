---
title: "API za proveru statusa mobilnog broja"
description: "Identity Authentication 集成指南 - Ace Data Cloud"
---

Ovaj dokument će predstaviti API za proveru statusa mobilnog broja, koji se može koristiti za verifikaciju statusa mobilnog broja, možete uneti mobilni broj za proveru.

## Proces prijave

Da biste koristili API, prvo idite na [API za proveru statusa mobilnog broja](https://platform.acedata.cloud/documents/037e2b5b-d0c0-425c-9e13-955ac64d1e07) i zatražite odgovarajuću uslugu, nakon što uđete na stranicu, kliknite na dugme „Acquire“, kao što je prikazano na slici:

![](https://cdn.acedata.cloud/q6ytrc.png)

Ako još niste prijavljeni ili registrovani, automatski ćete biti preusmereni na stranicu za prijavu koja vas poziva da se registrujete i prijavite, nakon prijave ili registracije automatski ćete se vratiti na trenutnu stranicu.

Prilikom prve prijave dobićete besplatnu kvotu, koju možete besplatno koristiti za ovaj API.

## Osnovna upotreba

Prvo se upoznajte sa osnovnim načinom korišćenja, jednostavno unesite mobilni broj i dobićete obrađeni rezultat, prvo je potrebno jednostavno proslediti polje `mobile`. Zatim možemo popuniti odgovarajući sadržaj na interfejsu, kao što je prikazano na slici:

<p><img src="https://cdn.acedata.cloud/r17llo.png" width="500" className="m-auto" /></p>

Možete videti da smo postavili Request Headers, uključujući:

- `accept`: kakav format odgovora želite da primite, ovde je uneto `application/json`, tj. JSON format.
- `authorization`: ključ za pozivanje API-ja, nakon prijave možete direktno izabrati.

Takođe je postavljen Request Body, uključujući:

- `mobile`: mobilni broj koji treba obraditi, obavezno polje.
- `encryption`: opcionalno, parametar za enkripciju osetljivih polja (ako je potrebno poslati šifrovani tekst).

Nakon izbora, možete primetiti da je sa desne strane generisan odgovarajući kod, kao što je prikazano na slici:

<p><img src="https://cdn.acedata.cloud/dpun55.png" width="500" className="m-auto" /></p>

Kliknite na dugme „Try“ da biste izvršili testiranje, kao što je prikazano na gornjoj slici, ovde smo dobili sledeći rezultat:

```json
{
  "result": "0",
  "description": "成功",
  "status_code": 0
}
```

Vraćeni rezultat ima više polja, opisana kao:

- `result`, kod rezultata verifikacije, naplata je sledeća.
  - Kodovi rezultata naplate:
    - 0: uspešno
  - Kodovi rezultata bez naplate:
    - -1: nije pronađen rezultat
    - -2: format mobilnog broja nije ispravan
    - -3: usluga verifikacionog centra je zauzeta
- `description`, opis poslovnog rezultata.
- `status_code`, kod statusa:
  - 0: normalno
  - 1: isključen
  - 2: otkazan broj
  - 3: nepostojeći broj
  - 4: nije u mreži
  - 99: nepoznati status

Može se primetiti da je status ovog mobilnog broja već proveren.

Takođe, ako želite da generišete odgovarajući kod za integraciju, možete ga direktno kopirati, na primer, CURL kod je sledeći:

```shell
curl -X POST 'https://api.acedata.cloud/identity/phone/check-1e' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "mobile": "***"
}'
```

Python kod za integraciju je sledeći:

```python
import requests

url = "https://api.acedata.cloud/identity/phone/check-1e"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "mobile": "***"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## Obrada grešaka

Kada pozivate API, ako naiđete na grešku, API će vratiti odgovarajući kod greške i informacije. Na primer:

- `400 token_mismatched`: Neispravna zahtev, moguće zbog nedostajućih ili neispravnih parametara.
- `400 api_not_implemented`: Neispravna zahtev, moguće zbog nedostajućih ili neispravnih parametara.
- `401 invalid_token`: Neautorizovano, neispravan ili nedostajući autorizacioni token.
- `429 too_many_requests`: Previše zahteva, premašili ste limit brzine.
- `500 api_error`: Interna greška servera, nešto nije u redu sa serverom.

### Primer greške u odgovoru

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Zaključak

Kroz ovaj dokument, upoznali ste se sa načinom korišćenja API-ja za proveru statusa mobilnog broja, možete uneti mobilni broj za proveru. Nadamo se da će vam ovaj dokument pomoći da bolje integrišete i koristite ovaj API. Ako imate bilo kakvih pitanja, slobodno kontaktirajte naš tim tehničke podrške.
