---
title: "价格总览"
description: "Ace Data Cloud 按量计费规则与查价入口。"
---

Ace Data Cloud 上的服务采用**按量计费**（Pay-as-you-go），不收取月度订阅费。

## 计费币种

- 控制台展示使用 **USD** 作为参考币种
- 实际扣减以**积分（Credits）**为单位
- 套餐购买时按当时套餐的「Credits ↔ USD」比例折算

USD 估算：

$$
\text{USD} = \text{Credits} \times \frac{\text{package.price}}{\text{package.amount}}
$$

当前主流套餐的换算比例约为 **$0.095215 / Credit**。

## 计费时机

| 调用类型 | 何时扣费 |
|---|---|
| 同步 API（如 Chat） | 响应返回时按 token / 字符 / 次数扣 |
| 异步任务（图像 / 视频 / 音频） | 任务最终 success 时扣费 |
| 任务最终 failed | 一般不扣费（以服务详情页约定为准） |
| 上游 5xx / 网关超时 | 不扣费 |

## 查看实时价格

价格随服务、模型、参数（分辨率、时长、token 数）变化，**最权威的当前价格在控制台**：

<Steps>
  <Step title="登录控制台">
    打开 [platform.acedata.cloud](https://platform.acedata.cloud)
  </Step>
  <Step title="进入服务详情页">
    在「Services」中选择目标服务（Midjourney、Suno、Claude 等）
  </Step>
  <Step title="查看定价">
    打开「定价」/「Pricing」标签，按场景查 USD 单价
  </Step>
</Steps>

<Note>
  历史上本文档区曾尝试维护分品类的价格表，但 AI 模型更新极快——为避免误导，目前**只把控制台作为唯一定价来源**。
</Note>

## 实例余额与通用余额

- **实例余额**（Instance / Dedicated）：在某个服务下创建的应用所分配的额度，只能用于该服务
- **通用余额**（General Balance）：所有服务通用
- 默认从实例余额扣减；若实例应用开启了「允许使用通用余额」，实例余额不足时会自动落到通用余额
- 余额不足时 API 返回 `403 used_up`（不是 402）——见 [响应格式](/concepts/responses)

## 折扣

质押 $ACE 代币可叠加折扣，相关入口与规则以控制台为准。

## 相关文档

<CardGroup cols={2}>
  <Card title="FAQ — 计费与额度" icon="circle-info" href="/faq/billing">
    Credits / USD 换算与常见疑问
  </Card>
  <Card title="响应格式" icon="brackets-curly" href="/concepts/responses">
    余额耗尽、内容审核等错误码
  </Card>
</CardGroup>
