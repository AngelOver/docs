---
title: "Nopea aloitus"
description: "Aloita XHuoAPI API:n käyttö 5 minuutissa"
---

## 1. Hanki API-avain

Rekisteröidy tilille osoitteessa [platform.xhuoapi.ai](https://api.xhuoapi.ai) ja luo API-todennustiedot.

<Steps>
  <Step title="Rekisteröidy tilille">
    Käy osoitteessa [platform.xhuoapi.ai](https://api.xhuoapi.ai) ja suorita rekisteröityminen.
  </Step>
  <Step title="Tilaa palvelu">
    Selaa saatavilla olevia palveluita ja klikkaa **Hanki** tilauksen tekemiseksi. Useimmat palvelut tarjoavat ilmaisen käyttörajan.
  </Step>
  <Step title="Luo todennustiedot">
    Mene palvelun **Todennustiedot** -sivulle ja luo API-token.
  </Step>
</Steps>

## 2. Lähetä ensimmäinen pyyntö

Kaikki API:t käyttävät Bearer Token -todennusta:

<CodeGroup>

```bash cURL
curl -X POST https://api.xhuoapi.ai/v1/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-sonnet-4-20250514",
    "messages": [{"role": "user", "content": "你好！"}]
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.xhuoapi.ai/v1/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"},
    json={
        "model": "claude-sonnet-4-20250514",
        "messages": [{"role": "user", "content": "你好！"}],
    },
)
print(response.json())
```

```javascript JavaScript
const response = await fetch("https://api.xhuoapi.ai/v1/v1/chat/completions", {
  method: "POST",
  headers: {
    Authorization: "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    model: "claude-sonnet-4-20250514",
    messages: [{ role: "user", content: "你好！" }],
  }),
});
const data = await response.json();
console.log(data);
```

</CodeGroup>

## 3. Tutustu lisää API:hin

<CardGroup cols={2}>
  <Card title="API-viite" icon="code" href="/authentication">
    Selaa ja testaa kaikkia API-päätepisteitä interaktiivisesti
  </Card>
  <Card title="Integrointiohjeet" icon="book" href="/guides/claude/claude_chat_completions">
    Yksityiskohtaiset integraatio-oppaat jokaiselle palvelulle
  </Card>
</CardGroup>