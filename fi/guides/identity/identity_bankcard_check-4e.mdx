---
title: "Pankkikortin neljän elementin tarkistus API -liittymän ohjeet"
description: "Identity Authentication 集成指南 - Ace Data Cloud"
---

Tässä artikkelissa esitellään pankkikortin neljän elementin tarkistus API -liittymän ohjeet, jota voidaan käyttää pankkikortin numeron, nimen, tilin avausasiakirjan numeron ja tilin avauspuhelinnumeron syöttämiseen, jotta voidaan tarkistaa tietojen aitous ja johdonmukaisuus.

## Hakuprosessi

API:n käyttämiseksi sinun on ensin mentävä [pankkikortin neljän elementin tarkistus API](https://platform.acedata.cloud/documents/02f77369-ec7b-4218-a956-d7a84982a626) -sivulle ja haettava vastaavaa palvelua. Sivulle päästyäsi napsauta "Acquire" -painiketta, kuten kuvassa näkyy:

![](https://cdn.acedata.cloud/q6ytrc.png)

Jos et ole vielä kirjautunut sisään tai rekisteröitynyt, sinut ohjataan automaattisesti kirjautumissivulle, jossa sinua kutsutaan rekisteröitymään ja kirjautumaan sisään. Kirjautumisen ja rekisteröitymisen jälkeen palaat automaattisesti nykyiselle sivulle.

Ensimmäisellä hakemiskerralla saat ilmaisia käyttöoikeuksia, joiden avulla voit käyttää tätä API:a ilmaiseksi.

## Peruskäyttö

Ensinnäkin on hyvä ymmärtää peruskäyttötapa, eli syöttämällä pankkikortin numerotiedot, voit saada käsitellyn tuloskuvan. Ensimmäiseksi sinun on yksinkertaisesti välitettävä `bank_card`-kenttä. Voimme seuraavaksi täyttää vastaavat tiedot käyttöliittymässä, kuten kuvassa näkyy:

<p><img src="https://cdn.acedata.cloud/1pl0y5.png" width="500" className="m-auto" /></p>

Voit nähdä, että olemme asettaneet pyyntöotsikot, mukaan lukien:

- `accept`: haluamasi vastausmuoto, tässä täytetään `application/json`, eli JSON-muoto.
- `authorization`: API:n kutsumiseen käytettävä avain, jonka voit valita suoraan alasvetovalikosta hakemisen jälkeen.

Lisäksi on asetettu pyyntökeho, joka sisältää:

- `bank_card`: käsiteltävä pankkikortin numerotieto, pakollinen parametri.
- `id_card`: tilin avausasiakirjan numero, pakollinen parametri.
- `name`: käyttäjän nimi, pakollinen parametri.
- `phone`: varattu puhelinnumero.
- `cert_type`: tilin avausasiakirjan tyyppi, joka on oltava sama kuin tilin avausasiakirjassa.
- `encryption`: valinnainen, arkaluontoisten kenttien salausparametri (jos salattua tietoa on lähetettävä).

Valinnan jälkeen voit huomata, että oikealla puolella on myös luotu vastaava koodi, kuten kuvassa näkyy:

<p><img src="https://cdn.acedata.cloud/slajgv.png" width="500" className="m-auto" /></p>

Napsauta "Try" -painiketta testataksesi, kuten yllä olevassa kuvassa näkyy, ja saamme seuraavat tulokset:

```json
{
  "result": "0",
  "description": "Todennus hyväksytty"
}
```

Palautetussa tuloksessa on useita kenttiä, jotka esitellään seuraavasti:

- `result`, todennustuloskoodi, maksutiedot seuraavasti.
  - Maksulliset tuloskoodit:
    - 0: Todennus hyväksytty
    - -1: Todennus ei hyväksytty
    - -4: Kortinhaltijan tiedot virheelliset
    - -5: Ei avattu kortittomalle maksulle
    - -6: Tämä kortti on takavarikoitu
    - -7: Virheellinen korttinumero
    - -8: Tälle kortille ei ole vastaavaa myöntäjää
    - -9: Tämä kortti ei ole alustettu tai se on lepotilassa
    - -10: Huijauskortti, kortti on nielty
    - -11: Tämä kortti on ilmoitettu kadonneeksi
    - -12: Tämä kortti on vanhentunut
    - -13: Rajoitettu kortti
    - -14: Salasanavirheiden määrä ylittää rajan
    - -15: Myöntäjä ei tue tätä transaktiota
  - Ilmaistulokset:
    - -2: Nimen tarkistus ei hyväksytty
    - -3: Pankkikortin numero virheellinen
    - -16: Vahvistuskeskus on ruuhkautunut
    - -17: Vahvistusmäärä ylittää rajan, yritä uudelleen seuraavana päivänä
- `description`, liiketoimintatuloksen kuvaus.

Voit nähdä, että tämän pankkikortin pankkikortin numero, nimi, tilin avausasiakirjan numero ja tilin avauspuhelinnumero ovat aitoja ja johdonmukaisia.

Jos haluat luoda vastaavan liittymiskoodin, voit kopioida sen suoraan, esimerkiksi CURL-koodi on seuraava:

```shell
curl -X POST 'https://api.acedata.cloud/identity/bankcard/check-4e' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "name": "***",
  "phone": "***",
  "id_card": "***",
  "bank_card": "***"
}'
```

Pythonin liittymiskoodi on seuraava:

```python
import requests

url = "https://api.acedata.cloud/identity/bankcard/check-4e"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "name": "胡印福",
    "phone": "***",
    "id_card": "***",
    "bank_card": "***"
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## Virheiden käsittely

API:a kutsuttaessa, jos kohtaat virheitä, API palauttaa vastaavat virhekoodit ja tiedot. Esimerkiksi:

- `400 token_mismatched`: Huono pyyntö, mahdollisesti puuttuvien tai virheellisten parametrien vuoksi.
- `400 api_not_implemented`: Huono pyyntö, mahdollisesti puuttuvien tai virheellisten parametrien vuoksi.
- `401 invalid_token`: Ei valtuutettu, virheellinen tai puuttuva valtuutusavain.
- `429 too_many_requests`: Liian monta pyyntöä, olet ylittänyt nopeusrajoituksen.
- `500 api_error`: Sisäinen palvelinvirhe, jotain meni pieleen palvelimella.

### Virhevastauksen esimerkki

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "haku epäonnistui"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Johtopäätös

Tämän asiakirjan avulla olet oppinut, kuinka käyttää pankkikortin neljän elementin tarkistus API:a pankkikortin numeron, nimen, tilin avausasiakirjan numeron ja tilin avauspuhelinnumeron syöttämiseen, jotta voidaan tarkistaa tietojen aitous ja johdonmukaisuus. Toivomme, että tämä asiakirja auttaa sinua paremmin liittymään ja käyttämään tätä API:a. Jos sinulla on kysymyksiä, ota rohkeasti yhteyttä tekniseen tukitiimiimme.
