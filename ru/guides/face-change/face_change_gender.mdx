---
title: "Инструкция по интеграции API для изменения пола лица"
description: "Face Transformation 集成指南 - Ace Data Cloud"
---

В этой статье будет представлена инструкция по интеграции API для изменения пола лица, который позволяет пользователю загрузить изображение лица и, основываясь на алгоритмах редактирования и генерации лиц, получить изображение с измененным полом. Изменение пола с мужского на женский может включать эффекты красоты, легкий макияж, челку и длинные волосы; изменение пола с женского на мужской может включать добавление бороды и укороченные волосы.

## Процесс подачи заявки

Чтобы использовать API, сначала необходимо перейти на соответствующую страницу [API для изменения пола лица](https://platform.acedata.cloud/documents/0e4e5318-060e-4976-8d3a-171301240f8c) и подать заявку на соответствующую услугу. После перехода на страницу нажмите кнопку «Acquire», как показано на изображении:

![](https://cdn.acedata.cloud/q6ytrc.png)

Если вы еще не вошли в систему или не зарегистрированы, вас автоматически перенаправит на страницу входа, где вам будет предложено зарегистрироваться и войти в систему. После регистрации и входа вы автоматически вернетесь на текущую страницу.

При первой подаче заявки будет предоставлен бесплатный лимит, который можно использовать для бесплатного доступа к этому API.

## Основное использование

Сначала ознакомьтесь с основным способом использования, который заключается в вводе ссылки на изображение и информации о поле, чтобы получить обработанное изображение. Сначала необходимо передать поле `image_url`, изображение лица показано на следующем изображении:

<p><img src="https://cdn.acedata.cloud/lrbtcn.jpg" width="500" className="m-auto" /></p>

Затем нам нужно загрузить параметры информации о поле лица `gender_infos`, который является массивом, в который можно передать несколько значений. Далее мы можем заполнить соответствующие данные на интерфейсе, как показано на изображении:

<p><img src="https://cdn.acedata.cloud/j22eg4.png" width="500" className="m-auto" /></p>

Как видно, здесь мы настроили заголовки запроса, включая:

- `accept`: формат ответа, который вы хотите получить, здесь указано `application/json`, то есть формат JSON.
- `authorization`: ключ API, который можно выбрать из выпадающего списка после подачи заявки.

Также настроено тело запроса, включая:

- `image_url`: ссылка на изображение лица, которое нужно обработать.
- `gender_infos`: информация о поле и области лица, где поле является обязательным параметром, 0 означает изменение с мужского на женский, 1 означает изменение с женского на мужской.

После выбора можно заметить, что справа также сгенерирован соответствующий код, как показано на изображении:

<p><img src="https://cdn.acedata.cloud/5kwudj.png" width="500" className="m-auto" /></p>

Нажмите кнопку «Try», чтобы провести тестирование, как показано на изображении, и мы получим следующий результат:

```json
{
  "image_url": "https://faceeffect-1254418846.cos.ap-guangzhou.myqcloud.com/ft/SwapGenderPic/1256437459/1d85ceb9-6847-41ec-a9fd-7a20ddbd13b6"
}
```

Как видно, в возвращенном результате есть поле `image_url`, которое представляет собой изображение лица, измененное в соответствии с введенными параметрами пола. Измененное изображение лица показано ниже:

<p><img src="https://cdn.acedata.cloud/hm7w44.png" width="500" className="m-auto" /></p>

Как видно, лицо на изображении изменилось в соответствии с введенным полом.

Кроме того, если вы хотите сгенерировать соответствующий код интеграции, вы можете просто скопировать его, например, код CURL выглядит следующим образом:

```shell
curl -X POST 'https://api.acedata.cloud/face/change-gender' \
-H 'accept: application/json' \
-H 'authorization: Bearer {token}' \
-H 'content-type: application/json' \
-d '{
  "image_url": "https://cdn.acedata.cloud/lrbtcn.jpg",
  "gender_infos": [{"gender":1}]
}'
```

Код интеграции на Python выглядит следующим образом:

```python
import requests

url = "https://api.acedata.cloud/face/change-gender"

headers = {
    "accept": "application/json",
    "authorization": "Bearer {token}",
    "content-type": "application/json"
}

payload = {
    "image_url": "https://cdn.acedata.cloud/lrbtcn.jpg",
    "gender_infos": [{"gender":1}]
}

response = requests.post(url, json=payload, headers=headers)
print(response.text)
```

## Обработка ошибок

При вызове API, если возникнет ошибка, API вернет соответствующий код ошибки и информацию. Например:

- `400 token_mismatched`: Неверный запрос, возможно, из-за отсутствующих или недействительных параметров.
- `400 api_not_implemented`: Неверный запрос, возможно, из-за отсутствующих или недействительных параметров.
- `401 invalid_token`: Неавторизовано, недействительный или отсутствующий токен авторизации.
- `429 too_many_requests`: Слишком много запросов, вы превысили лимит частоты.
- `500 api_error`: Внутренняя ошибка сервера, что-то пошло не так на сервере.

### Пример ответа об ошибке

```json
{
  "success": false,
  "error": {
    "code": "api_error",
    "message": "fetch failed"
  },
  "trace_id": "2cf86e86-22a4-46e1-ac2f-032c0f2a4e89"
}
```

## Заключение

С помощью этого документа вы узнали, как использовать API для изменения пола лица, позволяющий пользователю загрузить изображение лица и, основываясь на алгоритмах редактирования и генерации лиц, получить изображение с измененным полом. Надеемся, что этот документ поможет вам лучше интегрировать и использовать этот API. Если у вас есть какие-либо вопросы, пожалуйста, не стесняйтесь обращаться в нашу техническую поддержку.
