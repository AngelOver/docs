---
title: "Inizio Rapido"
description: "Inizia con XHuoAPI API in 5 minuti"
---

## 1. Ottenere la API Key

Registrati su [platform.xhuoapi.ai](https://api.xhuoapi.ai) e crea le credenziali API.

<Steps>
  <Step title="Registrazione">
    Visita [platform.xhuoapi.ai](https://api.xhuoapi.ai) per completare la registrazione.
  </Step>
  <Step title="Abbonamento al servizio">
    Esplora i servizi disponibili e clicca su **Ottieni** per completare l'abbonamento. La maggior parte dei servizi offre un piano gratuito.
  </Step>
  <Step title="Creazione delle credenziali">
    Vai alla pagina **Credenziali** del servizio e crea un Token API.
  </Step>
</Steps>

## 2. Invia la prima richiesta

Tutte le API utilizzano l'autenticazione Bearer Token:

<CodeGroup>

```bash cURL
curl -X POST https://api.xhuoapi.ai/v1/v1/chat/completions \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-sonnet-4-20250514",
    "messages": [{"role": "user", "content": "你好！"}]
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.xhuoapi.ai/v1/v1/chat/completions",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"},
    json={
        "model": "claude-sonnet-4-20250514",
        "messages": [{"role": "user", "content": "你好！"}],
    },
)
print(response.json())
```

```javascript JavaScript
const response = await fetch("https://api.xhuoapi.ai/v1/v1/chat/completions", {
  method: "POST",
  headers: {
    Authorization: "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    model: "claude-sonnet-4-20250514",
    messages: [{ role: "user", content: "你好！" }],
  }),
});
const data = await response.json();
console.log(data);
```

</CodeGroup>

## 3. Esplora altre API

<CardGroup cols={2}>
  <Card title="Riferimento API" icon="code" href="/authentication">
    Sfoglia e testa interattivamente tutti gli endpoint API
  </Card>
  <Card title="Guida all'integrazione" icon="book" href="/guides/claude/claude_chat_completions">
    Tutorial dettagliati per l'integrazione di ogni servizio
  </Card>
</CardGroup>